apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app
  namespace: support-ai
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  rules:
    - host: support.yengec.co
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: support-ai-app
                port:
                  number: 80
